snapshots:
    - name : dim_bookings
      relation : ref('bookings')        ## meaning : use ephemeral bookings model
      config :
        schema : gold
        database :  AIRBNB 
        unique_key : BOOKING_ID 
        strategy :  timestamp
        updated_at : BOOKING_CREATED_AT 
        dbt_valid_to_current : "to_date('9999-12-31')"  